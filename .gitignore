# ---------- OS ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ---------- Editors / IDEs ----------
.idea/
.vscode/
*.swp
*.swo
*~

# ---------- Node (保留的概率较低,但先列好) ----------
node_modules/
npm-debug.log*

# ---------- 背单词音频(P8 切片) ----------
# 音频由 scripts/import-vocab-pipeline.mjs 调 edge-tts 重新合成,
# 二进制大不入库;clone 后跑脚本一键重生。
public/audio/

# ---------- 背单词单词配图 ----------
# 由 /api/vocab-batch-image 调 MiniMax 生成,同为生成型二进制不入库,
# 可重复调用批量补图接口重生(只补无图核心词)。
public/images/words/
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Python (exam-analysis/ 下脚本运行时) ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
*.egg-info/
.coverage
htmlcov/

# ---------- 本地调试 / 临时产物 ----------
*.log
*.tmp
*.bak
*.orig
*.rej
.cache/
.output/
dist/
build/

# ---------- 保留的文件白名单(防止误忽略) ----------
# LICENSE 必须入库
!LICENSE

# ---------- 工具会话状态 ----------
.serena/

# ---------- 原型废弃实验(已弃用,不入库) ----------
prototype/.trash-*

# ---------- Next.js(M1 脚手架) ----------
/.next/
/out/
/coverage
*.pem
.env*
.vercel
*.tsbuildinfo
next-env.d.ts

# ---------- M1 工程产物(dev 与双击共存,见 docs/M1-实施计划.md) ----------
next-server/
/config.json
/data

# ---------- WorkBuddy 本地工作记忆 ----------
.workbuddy/

# (原型产物一律入 prototype/ 留痕,不再忽略 tmp/)
