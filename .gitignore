# ---------- OS ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ---------- Editors / IDEs ----------
.idea/
.vscode/
*.swp
*.swo
*~

# ---------- Node (保留的概率较低,但先列好) ----------
node_modules/
npm-debug.log*

# ---------- 背单词音频(P8 切片) ----------
# 音频由 scripts/import-vocab-pipeline.mjs 调 edge-tts 重新合成,
# 二进制大不入库;clone 后跑脚本一键重生。
public/audio/

# 例外:data/seed/ielts-100.txt 是配置型词表(几 KB),不是运行时数据,
# 入库方便 clone 后直接跑 pipeline。data/app.db 等运行时库仍被 data/ 整体排除。
!data/seed/
!data/seed/ielts-100.txt
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Python (exam-analysis/ 下脚本运行时) ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
*.egg-info/
.coverage
htmlcov/

# ---------- 本地调试 / 临时产物 ----------
*.log
*.tmp
*.bak
*.orig
*.rej
.cache/
.output/
dist/
build/

# ---------- 保留的文件白名单(防止误忽略) ----------
# LICENSE 必须入库
!LICENSE

# ---------- 工具会话状态 ----------
.serena/

# ---------- 原型废弃实验(已弃用,不入库) ----------
prototype/.trash-*

# ---------- Next.js(M1 脚手架) ----------
/.next/
/out/
/coverage
*.pem
.env*
.vercel
*.tsbuildinfo
next-env.d.ts

# ---------- M1 工程产物(dev 与双击共存,见 docs/M1-实施计划.md) ----------
next-server/
/config.json
/data
# 例外:data/seed/ 是配置型词表(几 KB),非运行时数据,入库便于 clone 后直接跑 pipeline。
# data/app.db 等运行时库仍被 /data 整体排除。
!/data/seed/
!/data/seed/ielts-100.txt

# ---------- WorkBuddy 本地工作记忆 ----------
.workbuddy/
