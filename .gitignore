# ---------- OS ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ---------- Editors / IDEs ----------
.idea/
.vscode/
*.swp
*.swo
*~

# ---------- Node (保留的概率较低,但先列好) ----------
node_modules/
npm-debug.log*

# ---------- 背单词音频(P8 切片) ----------
# 音频由 scripts/import-vocab-pipeline.mjs 调 edge-tts 重新合成(增量补缺),
# 但已生成的 mp3 必须入库 —— 桌面安装包首启依赖 next-server/public/audio/ 完整,
# postbuild.mjs 把整个 public/ 平铺进 next-server/public/,缺了就是 404。
# 体积大但不可省。clone 后如需补充生词音频,跑 import-vocab-pipeline.mjs(增量)。
# public/audio/

# ---------- 背单词单词配图 ----------
# 同样必须入库,装包首启依赖。
# public/images/words/
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Python (exam-analysis/ 下脚本运行时) ----------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
.env
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
*.egg-info/
.coverage
htmlcov/

# ---------- 本地调试 / 临时产物 ----------
*.log
*.tmp
*.bak
*.orig
*.rej
.cache/
.output/
dist/
build/

# ---------- 保留的文件白名单(防止误忽略) ----------
# LICENSE 必须入库
!LICENSE

# ---------- 工具会话状态 ----------
.serena/

# ---------- 原型废弃实验(已弃用,不入库) ----------
prototype/.trash-*

# ---------- 真题存档(云端不维护,本地保留) ----------
# 1) 体积大(100+ 个文件,含 mp3/网页/图片);2) 含第三方版权内容,不便入库;
# 3) 真题源由用户本地维护(剪贴板导入 / 手动添加),不入 git。
# clone 后通过 docs/windows-test-guide.md §2「导入真题」流程本地补齐。
questions/

# ---------- Next.js(M1 脚手架) ----------
/.next/
/out/
/coverage
*.pem
.env*
.vercel
*.tsbuildinfo
next-env.d.ts

# ---------- M1 工程产物(dev 与双击共存,见 docs/M1-实施计划.md) ----------
next-server/
/config.json
# data/ 是运行时生成目录(用户的 SQLite 学习库 + 大量临时备份),
# 全部不进 git。app.db seed 改放 src-tauri/seed-data/ 走 Tauri 资源流。
/data
/data/seed-data/

# ---------- WorkBuddy 本地工作记忆 ----------
.workbuddy/

# (原型产物一律入 prototype/ 留痕,不再忽略 tmp/)
data/iot/cookies.txt
questions.bak-manual-20260907/
